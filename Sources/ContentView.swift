import SwiftUI

struct ContentView: View {
    var body: some View {
        Button("OK") {
            // TODO: Add action
        }
        .buttonStyle(.borderedProminent)
    }
}

#Preview {
    ContentView()
}
